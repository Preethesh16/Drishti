# Secrets — NEVER commit
.env
*.key
secrets/
setu_vault.db
*_vault.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# Data — keep schema/sample, ignore large raw drops if needed (currently tracked: keep CSVs/KMLs)
# (Uncomment if data gets large)
# data/*.csv
# data/*.kml

# DB / runtime
*.sqlite
*.sqlite3
registry.db
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Streamlit
.streamlit/secrets.toml

# Claude local (keep settings.local.json out of shared history if you prefer; we keep it)
# .claude/settings.local.json
